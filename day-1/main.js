// function calculateBMI(name, weight, height) {

//     var bmi = weight / (height * height);

//     console.log(`${name} Your BMI is ${bmi} kg/m².`);

// }
// calculateBMI("Rumesh", 54, 1.8);


let multiNumbers = (num1, num2, num3) => {
    return num1 * num2 * num3;
}

console.log(multiNumbers(10, 30, 5));
